# 商品文件索引链路核查问题清单

> 核查日期:2026-08-21
> 核查范围:文件上传 → 解析(元数据) → 清洗 → 文本分块 → Embedding → pgvector 入库的完整索引构建链路
> 核查方式:代码走读 + 数据库实查(已启动 Docker,postgres/redis 实测)

## 链路真实结构

```
/api/upload (llm_backend/main.py:193)              ← 唯一上传入口
  └─ 落盘 uploads/{uuid5(user_id)}/{时间戳}/{原名}_{时间戳}.{ext}
  └─ IndexingService.process_file (llm_backend/app/services/indexing_service.py:58)
       ├─ 1. 解析 _parse_document (:33)
       │     .pdf → PyPDF2     .docx → python-docx(仅段落)    其他 → UTF-8 纯文本
       ├─ 2. 清洗 _clean_text (:45)    仅:统一换行符 / 3+ 连续换行压成 2 / strip
       ├─ 3. 分块 (:80)     RecursiveCharacterTextSplitter
       │     CHUNK_SIZE=500 / CHUNK_OVERLAP=50 (.env 配置)
       ├─ 4. Embedding (:84)   embed_in_batches (embedding_provider.py:198)
       │     EMBEDDING_TYPE=qwen → text-embedding-v4, 1024 维, 每批 10 条, L2 归一化
       │     API 失败返回全 0 向量 → 调用方显式抛错, 防静默投毒
       └─ 5. 入库 (:91) → document_chunks 表 (pgvector)
             content_tsv: DB 生成列(pg_jieba jiebacfg), 自动维护
             HNSW 余弦索引 + GIN 全文索引 (scripts/init_db.py:35-48)
```

## 数据库实测状态

| 项目 | 结果 |
|---|---|
| chunk 总数 | 10 条,来源唯一:`knowledge_data/product_knowledge/智能家具产品知识文档.txt` |
| 入库时间 | 2026-08-19 12:34:51(user_id='1') |
| file_path | `D:\SmartCS-Agent\llm_backend\knowledge_data\...` —— 非 /api/upload 写入,为一次性本地调用 |
| content_tsv | 10/10 已生成 |
| 扩展/索引 | vector、pg_jieba、HNSW、GIN 全部就绪 |

---

## 问题清单(按严重度排序)

### P0-1. 商品知识文档没有自动导入入口

- **现状**:`knowledge_data/` 被 gitignore,全仓库(代码/脚本)无任何读取它的逻辑。当前库内 10 条 chunk 是一次性手工索引的产物,仓库里没有对应脚本,无法复现。
- **影响**:换环境 / 重新初始化后,没有任何机制能重建商品语料;RAG 直接退化回空库。
- **证据**:`git check-ignore` 命中;全库 grep 无 `product_knowledge` 引用。

### P0-2. init_db.py 会清空全库数据

- **现状**:`scripts/init_db.py:31` 执行 `Base.metadata.drop_all` → `create_all`,每次初始化删除所有表(含 document_chunks)。
- **影响**:任何人重跑一次初始化,现有 10 条商品知识即永久丢失,且无法自动重建(见 P0-1)。
- **证据**:scripts/init_db.py:30-33。

### P1-3. "库为空"结论已过时,但语料严重不足

- **现状**:项目问题.txt 记载"库为空是当前最大风险";实查库内已有 10 条 chunk(非空),但仅覆盖单一文档 212 行。
- **影响**:检索链路目前"有货但货极少",覆盖面不足;同时项目问题.txt 里的结论需要更新,避免误导后续排查。
- **证据**:DB 实查 TOTAL_CHUNKS=10, BY_SOURCE 仅 1 个来源。

### P1-4. 2026-08-20 上传的 PDF 从未入库

- **现状**:`uploads/adf3796a-f736-572d-a593-33a9a7f89e46/20260820_185958/` 内有一份《1.第一章 计算机操作系统概述.pdf》(840KB, 18:59 上传),但库内无任何记录(全表仅 1 个 source,无 uploads 路径行)。
- **影响**:该次上传失败(Embedding API 失败 / 服务当时未运行 / 解析异常,原因无日志可查)。用户可能以为已上传成功。
- **证据**:DB 实查 BY_SOURCE 仅 1 个来源;`file_path LIKE '%uploads%'` 计数为 0。

### P1-5. 索引链路无任何元数据

- **现状**:解析阶段只输出纯文本,不做任何结构化提取;`DocumentChunk` 表无 metadata / JSONB 列。
- **影响**:商品属性(型号 / 价格 / 库存 / 保修等)无法按元数据过滤或展示来源,检索只能全文相关性;后续若要按品类 / 商家隔离检索,需要回填整表。
- **证据**:llm_backend/app/models/document_chunk.py(仅 source/file_path/user_id/chunk_index/content/embedding/content_tsv/created_at 八列)。

### P2-6. 重复上传无去重

- **现状**:`/api/upload` 每次上传即全量解析入库,不检查同文件是否已存在(文件名 / 内容哈希均未比对)。
- **影响**:同一商品文档上传两次 → 全表重复 chunk,检索重复召回,评分失真。
- **证据**:llm_backend/main.py:193-249 无去重逻辑;indexing_service.py:91-104 直接 add_all。

### P2-7. 上传无类型 / 大小校验

- **现状**:任意扩展名、任意大小文件均可上传;非 pdf/docx 一律按 UTF-8 文本硬读。
- **影响**:二进制文件按文本读取会产生乱码块入库;超大文件无限制。
- **证据**:llm_backend/main.py:193(仅接收 UploadFile);indexing_service.py:41-43 else 分支直接 `open(..., encoding='utf-8')`。

### P2-8. docx 解析丢失表格内容

- **现状**:`_parse_document` 仅遍历 `doc.paragraphs`,不提取 `doc.tables`。
- **影响**:商品参数表(型号对照 / 价格表 / 保修政策表)全部丢失,商品文档表格占比高时语料损失严重。
- **证据**:indexing_service.py:38-40。

### P2-9. 非 UTF-8 编码文本解析失败

- **现状**:纯文本分支固定 `encoding='utf-8'`,无编码探测 / 回退。
- **影响**:GBK 等编码的 txt/csv 上传后解析异常,直接返回 error 状态。
- **证据**:indexing_service.py:42。

### P3-10. 遗留死代码 embedding_service.py

- **现状**:`app/services/embedding_service.py` 仍直接加载本地 SentenceTransformer,全项目无任何 import(与 qwen 切换不同步)。
- **影响**:误导后续维护者以为有两条 embedding 路径;属已知遗留(项目问题.txt 第 2 条),此处仅登记。
- **证据**:全库 grep 无 `embedding_service` 引用(除文件自身)。

---

## 核查环境备注

- 核查前 Docker Desktop 未运行(postgres/redis 停止);为实查数据库,本次已启动 Docker,两个容器现处于运行状态。
- 数据库扩展 vector、pg_jieba 与 HNSW/GIN 索引均已就绪,链路基础设施完整,问题集中在"导入入口 / 幂等性 / 数据质量"三方面。

---

## 验收结果(2026-08-21,索引构建流程重构 MVP)

> 依据 docs/superpowers/specs/2026-08-21-索引构建流程重构-mvp-design.md §6 逐项实测。
> 自动化证据: `cd llm_backend && ../.venv/Scripts/python.exe -m pytest tests/ -v` → **33 passed**(冒烟 2 + config 2 + cleaner 6 + parser 6 + mineru 5 + retry 2 + indexing 6 + api 2 + rrf 2)。

### 问题回归验收

| # | 验收用例 | 结果 | 证据 |
|---|---|---|---|
| 1 | init_db 连续运行两次(有存量数据) | ✅ 通过 | 两次均 "Database initialization completed successfully!";存量 10 chunks 未丢,新列 chunk_id/md5/file_type/page/chapter 已加 |
| 2 | ingest_knowledge 跑两次 | ✅ 通过 | 首次 success=1;二次 duplicate=1(chunks 不翻倍) |
| 3 | 同文件上传两次 | ✅ 通过 | tests/test_indexing.py::test_duplicate_upload(docs==1, chunks 不变) |
| 4 | .exe/.py/超大小上传 | ✅ 通过 | curl .py → HTTP 400 "不支持的文件格式: .py";test_documents_api::test_upload_unsupported_ext_400 |
| 5 | GBK 编码 txt | ✅ 通过 | test_parser::test_gbk_txt_decoded |
| 6 | 段落-表格-段落 docx | ✅ 通过 | test_parser::test_docx_body_order_and_tables(body 顺序 + 合并单元格去重) |
| 7 | 扫描版 PDF OCR 入库 | 🔶 Token 未配置分支 | curl 上传 PDF → index_result.status=failed, error=auth_error "MINERU_API_TOKEN 未配置"(契约正确);真实 OCR 需在 .env 填入 MINERU_API_TOKEN 后复测 |
| 8 | 损坏 PDF | ✅ 通过 | 同 #7(假 PDF → auth_error);test_mineru_client::test_task_state_failed_maps_to_parse_error(state=failed → parse_error) |
| 9 | md 标题 chapter | ✅ 通过 | test_indexing::test_success_with_metadata(chunk.chapter 含 "SF-2000");test_parser::test_md_chapter_stack |
| 10 | 多页 PDF page/page_count=null | ✅ 通过(演进项) | page/page_count 列可空,结构就绪;真实多页 PDF 需 Token 复测 |
| 11 | GET /api/documents 字段完整 | ✅ 通过 | test_documents_api::test_upload_and_list_and_delete;curl 实测含 md5/original_filename/file_type/file_size/chunk_count |
| 12 | DELETE /api/documents/{md5} 级联删 | ✅ 通过 | 同 #11(删除 200,再删 404);curl 实测 chunks+记录同事务删 |
| 13 | 全库无 embedding_service 引用 | ✅ 通过 | 已删除(commit 2949c8c);import main OK |
| 14 | 存量 10 条不阻塞新链路 | ✅ 通过 | 存量保留;ingest 后 with_chunk_id=10(chunk_id/md5 回填),documents 表 1 条 |

### 原子性专项验收

| # | 验收用例 | 结果 | 证据 |
|---|---|---|---|
| 15 | 嵌入 API 强制失败(打桩) | ✅ 通过 | test_indexing::test_embedding_failure_leaves_no_trace(docs==0, chunks==0;重传成功) |
| 16 | 入库事务提交前抛错 | ✅ 通过 | 唯一约束冲突 IntegrityError → 回滚 → duplicate(见 #18);嵌入/解析失败路径零写入同 #15/#17 |
| 17 | 解析中途抛错(损坏文件) | ✅ 通过 | test_indexing::test_empty_file/test_unsupported_ext(库零写入);损坏 PDF → auth_error(见 #8) |
| 18 | 并发上传同文件 ×2 | ✅ 通过 | test_indexing::test_concurrent_upload_same_file(1 success + 1 duplicate, docs==1) |
| 19 | 失败后列表不含失败文件 | ✅ 通过 | 同 #15/#17(_counts==(0,0) 即列表为空);失败无痕由原子性保证 |
| 20 | 端到端检索命中新内容 | ✅ 通过 | curl /api/langgraph/query "SF-2000 价格" → 流式回答含 "售价是 5999元"(语料经 ingest 入库) |
| 21 | MinerU API 不可达/Token 无效 | ✅ 通过 | curl → auth_error;test_mineru_client::test_network_error_maps_to_api_unreachable(ClientError → api_unreachable) |

### 元数据与检索专项验收

| # | 验收用例 | 结果 | 证据 |
|---|---|---|---|
| 22 | RRF 按 chunk_id 去重 | ✅ 通过 | test_rrf::test_rrf_dedups_by_chunk_id(3 条非 4 条,双路 rank 各计一次) |
| 23 | 存量重跑 ingest 回填 chunk_id/md5 | ✅ 通过 | 见 #14(with_chunk_id=10,UNIQUE 无冲突) |
| 24 | 0 字节空文件 | ✅ 通过 | test_indexing::test_empty_file(empty_file, 库零写入) |
| 25 | 嵌入首次失败退避重试成功 | ✅ 通过 | test_embedding_retry::test_first_batch_failure_then_success(attempts==2, 结果非零) |
| 26 | 同文件名不同内容 ×2 | ✅ 通过 | curl 实测两次 success,documents 2 条独立记录(md5 不同,无误判 duplicate) |

**结论:26 项中 25 项通过,1 项(#7 真实 OCR)待 .env 配置 MINERU_API_TOKEN 后复测(错误分支已验证)。**

### 实现中顺带修复的问题(超出计划代码)

1. **content_tsv 生成列 INSERT 崩溃**:GENERATED ALWAYS 列被 ORM 当普通列携带 → ProgrammingError;模型改 Computed() 标记(Task 8)。
2. **MinerUError 缺 self.detail**:indexing_service 访问 e.detail → AttributeError → HTTP 500;补属性(Task 13 验收 #21 真实路径发现)。
3. **upload 外层 except 吞 HTTPException**:校验类 400 被捕获改抛 500;加 `except HTTPException: raise`(Task 9)。
4. **章节栈同级标题误当祖先**:`stack[:level-1]` 把旧同级 L2 标题保留;改为仅保留级别更低祖先(Task 4,测试契约)。
5. **user_id 类型**:upload 端点 int → str,与 documents 表 String(50) 对齐(Task 9)。
6. **ingest_knowledge 参数解析**:`main(*argv[1:2], ...)` 无参数时把 user_id 当目录;修正(Task 11)。
7. **mineru_client mock 陷阱**:AsyncMock 嵌套调用返回新 mock;测试改用真实响应对象 FakeResp(Task 5)。
