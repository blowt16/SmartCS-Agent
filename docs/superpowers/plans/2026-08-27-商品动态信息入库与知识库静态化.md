# 商品动态信息入库与知识库静态化 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 商品价格/库存入数据库 `product_price_stock` 表，参数/政策静态信息入两份 docx 进向量库，京东自营售后政策独立成文档。

**Architecture:** 动态信息（价格/库存）由 `import_product_price_stock.py` 从 TSV 幂等 upsert 入库；静态信息由两个构建脚本生成 docx（商品知识文档移除价格、自营商品售后引用独立政策文档；《京东自营售后政策.docx》独立生成，每章节 ≥450 字符防切分器混合块）。规格：`docs/superpowers/specs/2026-08-27-商品动态信息入库与知识库静态化-design.md`。

**Tech Stack:** SQLAlchemy(async, psycopg)、python-docx、pytest、pytest-asyncio、pgvector(已有)

**分支:** `feat/kb-product-dynamic`（当前可能在 feat/intent-recognition，每任务开始前 `git branch --show-current` 确认并切换）

---

### Task 1: `product_price_stock` 模型 + init_db 注册

**Files:**
- Create: `llm_backend/app/models/product_price_stock.py`
- Modify: `llm_backend/app/models/__init__.py`（导出新模型）
- Modify: `llm_backend/scripts/init_db.py`（import ProductPriceStock）

- [ ] **Step 1: 写失败测试** `llm_backend/tests/test_product_price_stock_model.py`

```python
"""模型定义测试(不连库,校验表名/约束/字段声明)"""
from app.models.product_price_stock import ProductPriceStock
from sqlalchemy import Integer, Numeric, String, UniqueConstraint


def test_table_name():
    assert ProductPriceStock.__tablename__ == "product_price_stock"


def test_unique_constraint():
    names = [c.name for c in ProductPriceStock.__table__.constraints
             if isinstance(c, UniqueConstraint)]
    assert "uq_product_price_stock_name" in names


def test_price_type():
    col = ProductPriceStock.__table__.c.current_price
    assert isinstance(col.type, Numeric)
    assert not col.nullable
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd llm_backend && ../.venv/Scripts/python.exe -m pytest tests/test_product_price_stock_model.py -v`
Expected: FAIL（ModuleNotFoundError: app.models.product_price_stock）

- [ ] **Step 3: 写模型实现**

Create `llm_backend/app/models/product_price_stock.py`（对齐现有 Document 模型风格）：

```python
from sqlalchemy import Column, DateTime, Integer, Numeric, String, UniqueConstraint, func

from app.core.database import Base


class ProductPriceStock(Base):
    """商品动态信息表:价格与库存(动态数据,与 docx 静态知识分层)"""

    __tablename__ = "product_price_stock"
    __table_args__ = (
        UniqueConstraint("product_name", name="uq_product_price_stock_name"),
    )

    id = Column(Integer, primary_key=True, index=True)
    product_name = Column(String(255), nullable=False)   # 与 TSV 商品名称完全一致(查询/join 键)
    category = Column(String(50), nullable=False)
    current_price = Column(Numeric(10, 2), nullable=False)
    stock_quantity = Column(Integer, nullable=False)     # 0 = 无货
    updated_at = Column(DateTime, server_default=func.now(), onupdate=func.now())
```

Modify `llm_backend/app/models/__init__.py`：在 `from app.models.document import Document  # noqa: F401` 后追加：

```python
from app.models.product_price_stock import ProductPriceStock  # noqa: F401
```

Modify `llm_backend/app/models/__init__.py` 的 `__all__`：

```python
__all__ = ["User", "Conversation", "Message", "DocumentChunk", "Document", "ProductPriceStock"]
```

Modify `llm_backend/scripts/init_db.py` 第 20 行 import 行，追加 `ProductPriceStock`：

```python
from app.models import User, Conversation, Message, DocumentChunk, Document, ProductPriceStock
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd llm_backend && ../.venv/Scripts/python.exe -m pytest tests/test_product_price_stock_model.py -v`
Expected: PASS（3 passed）

- [ ] **Step 5: 建表集成验证**

Run: `cd llm_backend && ../.venv/Scripts/python.exe -m scripts.init_db`
Expected: "Database initialization completed successfully!"

Run: `cd llm_backend && ../.venv/Scripts/python.exe -c "import sys, app.core.database; sys.path.insert(0,'.'); import asyncio; from sqlalchemy import text; from app.core.database import AsyncSessionLocal; asyncio.run((lambda: __import__('asyncio').ensure_future(None))() )"` —— 改为下方 SQL 查询：

```bash
cd llm_backend && ../.venv/Scripts/python.exe - <<'PY'
import asyncio, sys
sys.path.insert(0, '.')
import app.core.database  # Windows Selector 事件循环补丁
from sqlalchemy import text
from app.core.database import AsyncSessionLocal

async def main():
    async with AsyncSessionLocal() as s:
        r = await s.execute(text(
            "SELECT column_name, data_type FROM information_schema.columns "
            "WHERE table_name='product_price_stock' ORDER BY ordinal_position"))
        for row in r:
            print(row.column_name, row.data_type)
asyncio.run(main())
PY
```

Expected: 打印 id/product_name/category/current_price/stock_quantity/updated_at 六列

- [ ] **Step 6: Commit**

```bash
cd D:/SmartCS-Agent && git branch --show-current   # 必须为 feat/kb-product-dynamic,否则 git checkout feat/kb-product-dynamic
git add llm_backend/app/models/product_price_stock.py llm_backend/app/models/__init__.py llm_backend/scripts/init_db.py llm_backend/tests/test_product_price_stock_model.py
git commit -m "feat: product_price_stock 商品动态信息表——价格/库存入库模型+测试,init_db 注册建表"
```

---

### Task 2: `import_product_price_stock.py` 导入脚本

**Files:**
- Create: `llm_backend/scripts/import_product_price_stock.py`
- Test: `llm_backend/tests/test_import_price_stock.py`

- [ ] **Step 1: 写失败测试** `llm_backend/tests/test_import_price_stock.py`

```python
"""价格解析与库存填充规则测试(纯函数,不连库)"""
from decimal import Decimal

from scripts.import_product_price_stock import assign_stock, parse_price


def test_parse_price_single():
    assert parse_price("约5261") == Decimal("5261.00")


def test_parse_price_range_mean():
    assert parse_price("约4712-5154") == Decimal("4933.00")


def test_parse_price_with_suffix():
    assert parse_price("5274(券后)") == Decimal("5274.00")


def test_parse_price_dash_means_empty():
    assert parse_price("—") is None
    assert parse_price("") is None


def test_parse_price_double_range():
    assert parse_price("约791-872 元") == Decimal("831.50")


def test_assign_stock_zero_first_in_category():
    assert assign_stock("智能晾衣架", True) == 0
    assert assign_stock("智能门锁", True) == 0


def test_assign_stock_low_first_in_category():
    assert assign_stock("电动智能沙发", True) == 2
    assert assign_stock("智能床垫", True) == 3
    assert assign_stock("电动升降桌", True) == 5


def test_assign_stock_default():
    assert assign_stock("电动智能沙发", False) == 50
    assert assign_stock("智能窗帘", True) == 50
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd llm_backend && ../.venv/Scripts/python.exe -m pytest tests/test_import_price_stock.py -v`
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 3: 写实现**

Create `llm_backend/scripts/import_product_price_stock.py`：

```python
"""将 TSV 商品价格/库存导入 product_price_stock 表(按 product_name 幂等 upsert,可重复执行)。

用法:
  python -m scripts.import_product_price_stock
"""
import asyncio
import csv
import re
import sys
from decimal import Decimal
from pathlib import Path

ROOT_DIR = Path(__file__).resolve().parent.parent          # llm_backend
PROJECT_ROOT = ROOT_DIR.parent                             # 项目根
TSV_PATH = PROJECT_ROOT / "scripts" / "data" / "jd_smart_furniture.tsv"

sys.path.insert(0, str(ROOT_DIR))
import app.core.database  # noqa: E402 —— Windows Selector 事件循环补丁

from app.core.database import AsyncSessionLocal  # noqa: E402
from app.core.logger import get_logger  # noqa: E402
from app.models.product_price_stock import ProductPriceStock  # noqa: E402
from sqlalchemy import select  # noqa: E402
from sqlalchemy.dialects.postgresql import insert as pg_insert  # noqa: E402

logger = get_logger(service="import_price_stock")

COL_NAME, COL_CATEGORY, COL_PRICE, COL_AFTERSALES = "商品名称", "品类", "参考价格(元)", "售后服务"

# 测试库存填充规则(固定选取保证可复现):
# 品类行序第一款商品填 0 库存/少量,其余 50
ZERO_STOCK_CATEGORIES = ["智能晾衣架", "智能门锁"]
LOW_STOCK_CATEGORIES = {"电动智能沙发": 2, "智能床垫": 3, "电动升降桌": 5}


def parse_price(text: str) -> Decimal | None:
    """提取数字(含范围/券后等格式),多值取均值;无法解析返回 None。"""
    numbers = [float(m) for m in re.findall(r"\d+(?:\.\d+)?", text or "")]
    if not numbers:
        return None
    return Decimal(str(sum(numbers) / len(numbers))).quantize(Decimal("0.01"))


def assign_stock(category: str, is_first_in_category: bool) -> int:
    """库存填充规则:目标品类行序第一款填 0/少量,其余 50。"""
    if is_first_in_category and category in ZERO_STOCK_CATEGORIES:
        return 0
    if is_first_in_category and category in LOW_STOCK_CATEGORIES:
        return LOW_STOCK_CATEGORIES[category]
    return 50


def read_tsv_rows(path: Path) -> list[dict]:
    """读 TSV 并补充库存/价格字段;价格解析失败的行整行跳过(记警告)。"""
    with open(path, encoding="utf-8") as f:
        rows = list(csv.DictReader(f, delimiter="\t"))

    seen_categories: set[str] = set()
    result: list[dict] = []
    for idx, row in enumerate(rows, start=2):  # 2 起为 TSV 真实行号(表头占 1)
        category = row[COL_CATEGORY].strip()
        is_first = category not in seen_categories
        seen_categories.add(category)

        price = parse_price(row[COL_PRICE])
        if price is None:
            logger.warning("第 {} 行价格解析失败,整行跳过: {} ({})", idx, row[COL_NAME], row[COL_PRICE])
            continue
        result.append({
            "product_name": row[COL_NAME].strip(),
            "category": category,
            "current_price": price,
            "stock_quantity": assign_stock(category, is_first),
        })
    return result


async def upsert_rows(rows: list[dict]) -> int:
    """按 product_name 幂等 upsert(存在则更新价格/库存,不存在则插入)。"""
    async with AsyncSessionLocal() as s:
        for r in rows:
            stmt = pg_insert(ProductPriceStock).values(**r)
            stmt = stmt.on_conflict_do_update(
                index_elements=[ProductPriceStock.product_name],
                set_={
                    "category": stmt.excluded.category,
                    "current_price": stmt.excluded.current_price,
                    "stock_quantity": stmt.excluded.stock_quantity,
                },
            )
            await s.execute(stmt)
        await s.commit()
    return len(rows)


async def main():
    rows = read_tsv_rows(TSV_PATH)
    if not rows:
        logger.error("无可入库行(价格解析全部失败),终止")
        return 1
    count = await upsert_rows(rows)
    zero = [r["product_name"] for r in rows if r["stock_quantity"] == 0]
    low = [f"{r['product_name']}({r['stock_quantity']})" for r in rows if 0 < r["stock_quantity"] < 50]
    logger.info("入库完成: {} 行 | 零库存 {} 款 | 少量 {} 款 | 其余 50", count, len(zero), len(low))
    logger.info("零库存: {}", zero)
    logger.info("少量: {}", low)
    return 0


if __name__ == "__main__":
    sys.exit(asyncio.run(main()))
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd llm_backend && ../.venv/Scripts/python.exe -m pytest tests/test_import_price_stock.py -v`
Expected: PASS（9 passed）

- [ ] **Step 5: 集成验证**

Run: `cd llm_backend && ../.venv/Scripts/python.exe -m scripts.import_product_price_stock`
Expected: "入库完成: 50 行 | 零库存 2 款 | 少量 3 款 | 其余 50"

Run（幂等验证，再跑一次）: 同上命令
Expected: 再次输出"入库完成: 50 行"(行数不变)

Run（数据抽查）:

```bash
cd llm_backend && ../.venv/Scripts/python.exe - <<'PY'
import asyncio, sys
sys.path.insert(0, '.')
import app.core.database
from sqlalchemy import text
from app.core.database import AsyncSessionLocal

async def main():
    async with AsyncSessionLocal() as s:
        r = await s.execute(text("SELECT count(*) FROM product_price_stock"))
        print("总行数:", r.scalar())
        r = await s.execute(text(
            "SELECT product_name, current_price, stock_quantity FROM product_price_stock "
            "WHERE stock_quantity < 50 ORDER BY stock_quantity"))
        for row in r:
            print(" ", row.product_name[:30], "| 价格", row.current_price, "| 库存", row.stock_quantity)
asyncio.run(main())
PY
```

Expected: 总行数 50；5 款商品库存 < 50（2 款 0、2/3/5 各 1 款），价格数值正确

- [ ] **Step 6: Commit**

```bash
cd D:/SmartCS-Agent && git branch --show-current   # 确认 feat/kb-product-dynamic
git add llm_backend/scripts/import_product_price_stock.py llm_backend/tests/test_import_price_stock.py
git commit -m "feat: 商品价格/库存导入脚本——TSV解析(范围取均值)+测试库存填充,按商品名幂等upsert"
```

---

### Task 3: `build_smart_furniture_docx.py` 改造（移除价格 + 自营引用句）

**Files:**
- Modify: `scripts/build_smart_furniture_docx.py`

- [ ] **Step 1: 修改 `add_product`，移除价格段**

在 `scripts/build_smart_furniture_docx.py` 的 `add_product` 中，删除这段：

```python
    if row[COL_PRICE].strip():
        doc.add_paragraph(f"参考价格：{row[COL_PRICE].strip()} 元（活动/参考价，以京东实时结算为准）")
```

- [ ] **Step 2: 修改售后章节为"自营引用句 / 原文要点"双分支**

将 `add_product` 中的售后章节段替换为：

```python
    # 售后服务（京东自营 → 引用独立政策文档；否则原文要点）
    aftersales = row[COL_AFTERSALES].strip()
    if aftersales == "京东自营":
        doc.add_heading("售后服务", level=4)
        doc.add_paragraph(
            "本商品为京东自营，适用《京东自营售后政策》（独立知识文档，已随知识库上传）",
            style="List Bullet",
        )
    elif aftersales:
        doc.add_heading("售后服务", level=4)
        for item in split_bullets(aftersales):
            doc.add_paragraph(item, style="List Bullet")
```

- [ ] **Step 3: 文档说明补充动态信息说明**

在 `main()` 中 `doc.add_paragraph(本文档涵盖...整理日期...)` 之后追加：

```python
    doc.add_paragraph(
        "商品价格与库存为动态信息，存储于系统数据库中（product_price_stock 表），本文档不包含。"
    )
```

- [ ] **Step 4: 重生成并验证**

Run: `cd D:/SmartCS-Agent && .venv/Scripts/python.exe scripts/build_smart_furniture_docx.py`
Expected: "完成: 生成 ...京东智能家具产品知识文档.docx（50 款商品, 9 个品类）"

Run（结构验证）:

```bash
cd D:/SmartCS-Agent && .venv/Scripts/python.exe - <<'PY'
from collections import Counter
from docx import Document

doc = Document("llm_backend/knowledge_data/product_knowledge_docx/京东智能家具产品知识文档.docx")
styles = Counter(p.style.name for p in doc.paragraphs)
texts = [p.text for p in doc.paragraphs]

# 1. 无价格段落
assert not any("参考价格" in t for t in texts), "仍有参考价格段落!"
# 2. 自营引用句出现 8 次(8 款京东自营商品)
refs = [t for t in texts if "适用《京东自营售后政策》" in t]
assert len(refs) == 8, f"自营引用句应为 8 条,实际 {len(refs)}"
# 3. 动态信息说明存在
assert any("动态信息" in t for t in texts), "缺少动态信息说明"
# 4. 样式分布(与旧版一致: H1=1 H2=9 H3=50 H4=250 - 价格移除后 H4 不变)
print("样式分布:", dict(styles))
print("自营引用句:", refs[:3])
print("✓ 结构验证通过")
PY
```

Expected: "✓ 结构验证通过"，H1=1 / H2=9 / H3=50 / H4=250，自营引用 8 条

- [ ] **Step 5: Commit**

```bash
cd D:/SmartCS-Agent && git branch --show-current   # 确认 feat/kb-product-dynamic
git add scripts/build_smart_furniture_docx.py
git commit -m "feat: 商品知识文档构建改造——移除价格段(动态信息入库),自营商品售后引用独立政策文档"
```

---

### Task 4: 京东自营售后政策官方检索（内容调研）

**Files:**
- 无代码文件；产出为 Task 5 的 `POLICY_SECTIONS` 内容依据

- [ ] **Step 1: 检索京东官方售后政策**

用 WebSearch 检索 `京东自营 售后政策 七天无理由 价格保护 帮助中心`，用 WebFetch 拉取 help.jd.com 相关页面（如 https://help.jd.com/user/issue/list-423.html 或检索到的官方政策页），提取以下主题的官方条款要点：
1. 七天无理由退货（适用范围、条件、类目例外）
2. 价格保护（价保期、申请方式、例外）
3. 售后流程（申请→审核→取件→处理）
4. 运费与上门取件（质量问题/无理由的运费承担）
5. 商品类目差异（大家电 30 天退货等）

- [ ] **Step 2: 整理为每章 5-6 条、单章 ≥450 字符的条款草稿**

每条要点对应官方表述，来源链接存档。产出整理结果记录在 Task 5 的 `POLICY_SECTIONS` 常量（如检索结果与草稿有出入，以官方条款为准修订）。

- [ ] **Step 3: 无代码提交（如内容已有草稿则跳过本步）**

若检索产出需要存档：`git commit -m "docs: 京东自营售后政策检索笔记——官方条款要点整理"`（文件放 `docs/superpowers/notes/`）

---

### Task 5: `build_jd_aftersales_docx.py` 新建（独立售后政策文档）

**Files:**
- Create: `scripts/build_jd_aftersales_docx.py`
- Test: `llm_backend/tests/test_aftersales_docx_build.py`

- [ ] **Step 1: 写失败测试** `llm_backend/tests/test_aftersales_docx_build.py`

```python
"""售后政策文档构建测试:章节 ≥450 字符约束(防切分器混合块,spec §3.3)"""
from scripts.build_jd_aftersales_docx import POLICY_SECTIONS, TOTAL_LENGTH_MIN


def test_each_section_at_least_450_chars():
    """每章节(标题+条款+分隔符)≥450 字符,强制章节边界成为切块边界"""
    for title, bullets in POLICY_SECTIONS:
        n = len(title) + sum(len(b) + 2 for b in bullets)
        assert n >= 450, f"章节 [{title}] 仅 {n} 字符,不足 450,会与相邻章节合并成混合块"


def test_doc_structure_contains_required_sections():
    titles = [t for t, _ in POLICY_SECTIONS]
    for required in ["七天无理由退货", "价格保护", "售后流程", "运费", "类目差异"]:
        assert any(required in t for t in titles), f"缺少章节: {required}"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd llm_backend && ../.venv/Scripts/python.exe -m pytest tests/test_aftersales_docx_build.py -v`
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 3: 写实现**

Create `scripts/build_jd_aftersales_docx.py`（条款内容以 Task 4 官方检索为准，草稿基于京东公开售后政策整理；每章 5-6 条、≥450 字符；总述段 + 来源链接）：

```python
"""
生成《京东自营售后政策.docx》独立知识文档(与商品知识文档同目录,一并上传索引)。

文档结构(与商品文档同款式体系,样式/召回约束见 spec §3.2/§3.3):
  H1 京东自营售后政策
  ├─ 总述段(Normal): 适用范围
  ├─ H2 章节 ×5(每章 ≥450 字符,含标题与条款,强制独立成块)
  │   └─ List Bullet 条款(高频业务词,提升 BM25 命中)
  └─ H2 参考来源(Normal 链接)

用法:
  python scripts/build_jd_aftersales_docx.py
输出: llm_backend/knowledge_data/product_knowledge_docx/京东自营售后政策.docx (gitignore 内)
"""

import argparse
from pathlib import Path

from docx import Document

PROJECT_ROOT = Path(__file__).resolve().parent.parent
OUTPUT_DIR = PROJECT_ROOT / "llm_backend" / "knowledge_data" / "product_knowledge_docx"
DOC_TITLE = "京东自营售后政策"

# 总述段(桥接块: "XX商品+售后主题" query 的召回入口,spec §3.3)
PREAMBLE = (
    "本政策适用于所有京东自营商品（商品信息中标明“京东自营”的商品）。"
    "京东自营商品由京东直接销售并提供售后，以下为售后政策要点整理，"
    "具体以订单售后页面与京东平台最新政策为准。"
)

# 每章节 (标题, [条款,...]); 单章(含标题与条款)≥450 字符约束由测试保证
POLICY_SECTIONS = [
    ("一、七天无理由退货", [
        "京东自营商品自签收次日起 7 天内支持无理由退货，法律规定或平台明确不可退货的商品除外",
        "退货需商品完好、附件齐全、不影响二次销售；定制类商品、已激活拆封的数码产品不适用",
        "家具类商品安装后不影响七天无理由退货，但需保持商品完好，包装与配件完整",
        "申请路径：订单详情 → 申请售后 → 选择“七天无理由退货” → 填写原因提交",
        "审核通过后按原支付路径退款；申请时请确认收货地址与取件方式",
        "无理由退货的寄回运费由买家承担（个别品类免运费），详见运费章节",
    ]),
    ("二、价格保护", [
        "京东自营商品支持价格保护：一般商品价保期 7 天，大家电等部分品类价保期 30 天",
        "价保期内商品降价，可在线申请退还差价，差价按价保期内订单价格差额计算",
        "申请路径：订单详情 → 申请价保 → 系统自动核算应退差价并原路退回",
        "价保不适用于限时秒杀、抢购等特殊促销活动商品，具体以页面规则为准",
        "价保期内同一商品仅可申请一次价保，以提交时间对应的促销价格核算",
        "价保申请无需联系客服，系统自动核算；如有疑问可在售后单中留言",
    ]),
    ("三、售后流程", [
        "在线申请：订单详情 → 申请售后 → 选择服务类型（退货/换货/维修）",
        "审核：京东受理并审核申请，通常 1 个工作日内完成审核并反馈结果",
        "取件/寄回：审核通过后京东安排上门取件，或用户按指引自行寄回商品",
        "处理：商品检测后按申请执行退款/换新/维修，退款原路退回",
        "售后单进度可在“我的订单 → 售后/退款”中实时查看，处理时效以售后单状态为准",
        "换货商品由京东重新发货；维修类售后由品牌售后网点或京东服务商处理",
    ]),
    ("四、运费与上门取件", [
        "京东自营商品因质量问题退换货，往返运费由京东承担，不收取用户任何运费",
        "七天无理由退货由买家承担寄回运费；个别品类免运费，以申请页面提示为准",
        "京东上门取件覆盖范围以收货地址为准；超出范围可自行寄回并上传物流单号",
        "运费争议以京东售后系统审核结果为准，可在线查询运费明细",
        "大件商品（如沙发、床等家具）由京东物流上门取件，无需用户自行搬运",
        "取件时间可在线预约，支持改期；长时间无法取件时客服会电话联系协调",
    ]),
    ("五、商品类目差异说明", [
        "大家电类支持 30 天质量问题退货、180 天质量问题换货（京东自营标准）",
        "智能家具类商品适用上述通用政策；安装类商品安装后仍可申请七天无理由退货",
        "具体适用条款以订单售后页面与京东平台最新政策为准，本文档为整理摘要",
        "商品页面标注的专属售后承诺（如质保年限、免费安装）优先于通用政策",
        "定制类、拆封影响二次销售的商品不支持无理由退货，质量问题仍可售后",
    ]),
]

# 每章节字符数参考线(测试断言 ≥450)
TOTAL_LENGTH_MIN = 450

SOURCE_SECTIONS = [
    ("京东帮助中心-售后政策", "https://help.jd.com/"),
]


def build_docx(out_path: Path) -> None:
    doc = Document()
    doc.add_heading(DOC_TITLE, level=1)
    doc.add_paragraph(PREAMBLE)

    for title, bullets in POLICY_SECTIONS:
        doc.add_heading(title, level=2)
        for b in bullets:
            doc.add_paragraph(b, style="List Bullet")

    doc.add_heading("参考来源", level=2)
    for name, url in SOURCE_SECTIONS:
        doc.add_paragraph(f"{name}: {url}")

    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
    doc.save(str(out_path))


def main():
    parser = argparse.ArgumentParser(description="生成京东自营售后政策知识文档")
    args = parser.parse_args()
    out_path = OUTPUT_DIR / f"{DOC_TITLE}.docx"
    build_docx(out_path)
    total = sum(len(title) + sum(len(b) + 2 for b in bullets)
                for title, bullets in POLICY_SECTIONS)
    print(f"完成: 生成 {out_path}（{len(POLICY_SECTIONS)} 个章节, 条款总长 {total} 字符）")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd llm_backend && ../.venv/Scripts/python.exe -m pytest tests/test_aftersales_docx_build.py -v`
Expected: PASS（2 passed）

- [ ] **Step 5: 生成文档 + 切块独立性验证**

Run: `cd D:/SmartCS-Agent && .venv/Scripts/python.exe scripts/build_jd_aftersales_docx.py`
Expected: "完成: 生成 ...京东自营售后政策.docx（5 个章节...）"

Run（章节独立成块验证，复刻索引管道切分逻辑）:

```bash
cd D:/SmartCS-Agent && .venv/Scripts/python.exe - <<'PY'
import sys
sys.path.insert(0, "llm_backend")
from app.services.doc_parser import parse_docx
from app.services.text_cleaner import clean_text
from app.services.indexing_service import IndexingService
from langchain_core.documents import Document as LD

path = "llm_backend/knowledge_data/product_knowledge_docx/京东自营售后政策.docx"
segments = parse_docx(path)
clean = [(clean_text(s.text), s.chapter) for s in segments if clean_text(s.text)]
full = "\n\n".join(t for t, _ in clean)
docs = IndexingService().text_splitter.split_documents([LD(page_content=full)])
print(f"全文 {len(full)} 字符 → {len(docs)} 块")
for i, d in enumerate(docs):
    print(f"  块{i}: {d.page_content[:26]!r} ... {d.page_content[-18:]!r}")
PY
```

Expected: 5 个章节各自独立成块（块首分别为"一、七天无理由/二、价格保护/三、售后流程/四、运费/五、类目"对应的块），无跨章节混合块

- [ ] **Step 6: Commit**

```bash
cd D:/SmartCS-Agent && git branch --show-current   # 确认 feat/kb-product-dynamic
git add scripts/build_jd_aftersales_docx.py llm_backend/tests/test_aftersales_docx_build.py
git commit -m "feat: 京东自营售后政策独立文档构建脚本——5章节各≥450字符强制独立成块,含总述段与来源"
```

---

### Task 6: 全链路验证 + 提交推送

**Files:** 无新文件；全链路回归

- [ ] **Step 1: 全量测试**

Run: `cd llm_backend && ../.venv/Scripts/python.exe -m pytest tests/ -q`
Expected: 全部通过（或与基线一致的失败集，确认无新增失败）

- [ ] **Step 2: 知识库双文档上传索引（可选，需 Embedding API）**

若需要端到端验证召回（参考 spec §3.3 实测方法），上传两份文档并抽查 3 条 query：
- "芝华仕沙发退货怎么退" → 政策块进 top-3
- "京东自营商品售后流程是什么" → 政策块 top-1
- "这款沙发支持七天无理由退货吗" → 政策块进 top-3

Run: 通过 `/api/upload` 或 `IndexingService.process_file` 上传
Expected: 政策块稳定召回

- [ ] **Step 3: 提交推送（先确认分支！）**

```bash
cd D:/SmartCS-Agent && git branch --show-current   # 必须为 feat/kb-product-dynamic
git add -A
git commit -m "feat: 商品动态信息入库与知识库静态化——价格/库存入库+售后政策独立文档+知识文档改造"
git push -c http.sslBackend=schannel origin feat/kb-product-dynamic
```

Expected: 推送成功；告知用户分支与提交概要

---

## Self-Review

**Spec 覆盖：**
- §1 product_price_stock 表 → Task 1 ✓
- §2 导入脚本（价格解析/库存填充/幂等）→ Task 2 ✓
- §3.1 商品文档改造（移除价格/自营引用/动态说明）→ Task 3 ✓
- §3.2 独立售后政策文档 → Task 4（内容检索）+ Task 5（构建）✓
- §3.3 召回约束（章节 ≥450/总述段/高频词）→ Task 5（PREAMBLE + POLICY_SECTIONS + 测试）✓
- §4 验证（建表/50行/幂等/docx 结构/切块）→ 各 Task 验证步骤 ✓
- §5 分支与提交 → Task 6 ✓

**占位符扫描：** 无 TBD/TODO；Task 4 条款以官方检索校准，草稿已可执行。

**类型一致性：** `parse_price`/`assign_stock` 签名在 Task 2 测试与实现一致；`POLICY_SECTIONS`/`PREAMBLE` 在 Task 5 测试与实现一致；`ProductPriceStock` 在 Task 1/2 一致。

**已知注意点：**
- 每个提交前必须 `git branch --show-current` 确认（用户多窗口开发，分支可能被外部切换）
- 推送用 `-c http.sslBackend=schannel`（本机 openssl 证书包失效）
- Windows 下脚本先 `import app.core.database`（Selector 事件循环补丁）
